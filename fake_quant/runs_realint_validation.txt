# Real integer validation: measure effect of int16 quantize/dequantize on proj_ex=16 activations
# Format: name: MODE [args for dart_gptq_wxaykvz.sh, without -g]
#
# With --realint, the bits==16 passthrough is replaced by real int16 quantize→dequantize.
# This affects: down_proj input (proj_ex=16), k/v cache (kv_ex=8 → already <16, unaffected),
# and any other quantizer set to 16 bits.
# Expected: small degradation from int16 rounding on down_proj activations.
# Compare against runs_fp32_validation.txt (same runs without --realint).
#
# Usage: python run_experiments.py --runfile runs_realint_validation.txt -g 2,3,4

